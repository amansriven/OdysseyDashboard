# deps -- never commit; reinstall with `npm i` / `pip install`
node_modules/
.vite/
dist/

# python
__pycache__/
*.pyc

# secrets / local config -- never commit; use .env.example as the template
.env
**/.env
!.env.example

# macOS
.DS_Store
